#include<stdio.h>
/**
 * main - First point
 * program reverse alphabet print
 * Return: Always 0 (Success)
*/
int main(void)
{
	char ch

	for (ch  = 'z'; ch  >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
