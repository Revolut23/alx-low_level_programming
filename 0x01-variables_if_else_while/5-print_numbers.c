#include<stdio.h>
/**
 * main - First point
 * program which prints all single number on the base 10
 * Return: Always 0 (Succes)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
