#include<stdio.h>
/**
 * main - First point
 * program which prints all single numbers base 10
 * Return: always 0 (Success)
 */
int main(void)
{
	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
