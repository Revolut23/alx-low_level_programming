#include<stdio.h>
/**
 * main - First point
 * program which prints all single numbers base 10
 * Return: always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <=  9;  n++)
	{
		putchar(n);
	}
	putchar("\n");
	return (0);
}
